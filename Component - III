import React, { FC } from "react";
import { StyleSheet, Text, View } from "react-native";
import { Colors } from "../../../theme/colors";

interface Props {
  mainTitle: string;
}

const App: FC<Props> = (props) => {
  return <Text style={styles.title}>{props.mainTitle}</Text>;
};

export default App;

const styles = StyleSheet.create({
  title: {
    fontSize: 26,
    color: Colors.white,
    textAlign: "center",
  },
  main_title: { alignSelf: "center" },
});
